BRIJWASI Cart Service

Setup:
1. Create PostgreSQL database `cart_db` locally and set credentials in `application.yml`.
2. Ensure Redis is running locally on port 6379.
3. Start Eureka then run this service.

Run:
   mvn clean package
   mvn spring-boot:run
