BRIJWASI Auth Service

Setup:
1. Create PostgreSQL database `auth_db` locally and set credentials in `application.yml`.
2. Build and run after Eureka is up.

Run:
   mvn clean package
   mvn spring-boot:run
