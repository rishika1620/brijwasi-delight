BRIJWASI Menu Service

Setup:
1. Create PostgreSQL database `menu_db` locally and set credentials in `application.yml`.
2. Start Eureka then run this service.

Run:
   mvn clean package
   mvn spring-boot:run
