package com.brijwasi.menu.repository;

import com.brijwasi.menu.model.AddOn;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AddOnRepository extends JpaRepository<AddOn, Long> {
}
